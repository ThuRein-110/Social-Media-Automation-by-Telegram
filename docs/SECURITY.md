# Security

Security controls include SSRF blocking, private-IP checks, same-origin redirect validation, schema validation, permission checks, audit logs, idempotent publishing jobs, and secret-free logging.

Never store social passwords. Never commit credentials.
