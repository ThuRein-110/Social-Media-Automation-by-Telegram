# Social APIs

Publishing adapters expose `validate()`, `publish()`, `getStatus()`, and `getAnalytics()`.

Development uses mock publishing. Live adapters must use official APIs and OAuth, and require explicit user approval before the first real post.
