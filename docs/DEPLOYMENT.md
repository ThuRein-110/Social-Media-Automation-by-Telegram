# Deployment

Deployment is pending explicit approval. Before deployment, choose the lowest-cost platform compatible with the final app, configure server-side secrets, keep production publishing disabled until approved, and verify webhook signatures.
