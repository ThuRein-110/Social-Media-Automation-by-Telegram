# Autopilot

Autopilot can select media, plan content, render, validate, schedule, publish, collect analytics, and learn only inside granted platforms, posting limits, cost limits, topics, and brand rules.

`/emergency_stop` disables publishing, cancels pending publish jobs where safe, and preserves logs/data.
